#include "main.h"
#include <stdio.h>

/**
 * mul - multipies two integers
 * @a: - first integer
 * @b: - second integer
 * Return: always 0
 */

int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
